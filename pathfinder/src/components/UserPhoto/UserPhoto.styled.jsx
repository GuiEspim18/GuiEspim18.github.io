import styled from "styled-components";
import { bgColor } from "../../styles/colors";

export const BackgroundUserPhoto = styled.span`
    background-color: ${bgColor};
    width: 45px;
    height: 45px;
    border-radius: 45px;
    display: flex;
    align-items: center;
    justify-content: center;
`;